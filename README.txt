This plugin manages packages and modules of QGIS python.

Features:

  * Show all the installed packages (distributions).
  * Show modules in a selected package.
  * Install a package/packages.
  * Load a module and get the version.
